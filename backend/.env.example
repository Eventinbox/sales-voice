PORT=4000
DATABASE_URL="file:./dev.db"

# Get a key from https://aistudio.google.com/apikey — leave blank to fall
# back to the regex heuristics in src/lib/parseMessage.ts
GEMMA_API_KEY=
# Optional — defaults to gemma-4-26b-a4b-it if unset
GEMMA_MODEL=gemma-4-26b-a4b-it
